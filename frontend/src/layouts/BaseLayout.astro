---
import '../styles/global.css';

interface Props {
	title: string;
	command: string;
	mode?: string;
	status?: string;
	editHref?: string;
	backHref?: string;
	noindex?: boolean;
}

const { title, command, mode = 'NORMAL', status, editHref, backHref, noindex = false } = Astro.props;
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" sizes="48x48" />
		<link rel="apple-touch-icon" href="/apple-touch-icon.png" />
		<meta name="viewport" content="width=device-width" />
		{noindex && <meta name="robots" content="noindex, follow" />}
		<title>{title}</title>
		<script defer src="https://m.masoko.net/core-util" data-website-id="b005feb8-941a-4967-b499-0a0b5f2eff9e"></script>
	</head>
	<body>
		<div class="term">
			<div class="term-bar">
				<div class="term-dots"><span></span><span></span><span></span></div>
				<p class="term-title"><span class="user">visitor@cli.masoko.net</span>:~$ {command}</p>
				{backHref && <a class="back" href={backHref}>&larr; cd ..</a>}
			</div>
			<div class="term-body">
				<slot />
			</div>
			<footer class="statusline">
				<span class="mode">{mode}</span>
				<span class="seg path">~/cli.masoko.net</span>
				{status && <span class="seg">{status}</span>}
				<span class="seg right">
					{editHref && (
						<>
							<a href={editHref}>edit</a>
							<span class="sep">·</span>
						</>
					)}
					<a href="/submit">submit</a>
					<span class="sep">·</span>
					<a href="https://github.com/hjelev/cli" target="_blank" rel="noopener">github</a>
				</span>
			</footer>
		</div>
	</body>
</html>
